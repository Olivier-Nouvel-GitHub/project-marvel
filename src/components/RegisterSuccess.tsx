import styled from "styled-components";

const Container = styled.div``;

export const RegisterSuccess = () => {
  return (
    <Container>
      Votre compte a été crée. Vous pouvez désormais vous connecter avec ces
      identifiants en cliquant sur l'image de profil en haut à droite.
    </Container>
  );
};
