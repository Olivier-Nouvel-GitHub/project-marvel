import styled from "styled-components";

const HeroStyle = styled.div`
  color: #5843e4;
  font-size: 22px;
`;

export const Hero = () => {
  return (
    <HeroStyle>
      <div>
        <div></div>
      </div>
    </HeroStyle>
  );
};
